# ── Video files (tracked via Git LFS instead) ──
# *.mp4 — intentionally NOT ignored, handled by Git LFS
*.mov
*.avi
*.mkv
*.webm

# ── Generated QR code output ──
qrcode_output.png

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# ── Node.js (if you later use the Node QR script) ──
node_modules/

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor folders ──
.vscode/
.idea/
